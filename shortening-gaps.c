#include <stdio.h>

main()

{
    int c, c_prev
    printf('Give me an input: ');

    /* Receive the input:*/
    while ( (c = getchar()) != EOF) {
        /* Check whether character is space*/
        if c = ' '
    }
}

